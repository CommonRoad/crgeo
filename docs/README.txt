CommonRoad-Geometric
==========================

Structure
---------

Sphinx and ReST is used to write the documentation. All source files are in the folder source.

Run `pip install -r docs/requirements.txt` before compiling the documentation with `make html`.

Recommended add-on for modifying the docs: https://github.com/prkumar/sphinx-reload